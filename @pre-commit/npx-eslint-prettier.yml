pre-commit:
  parallel: false
  jobs:
    - name: lint
      run: npx eslint --no-warn-ignored --fix {staged_files}
      stage_fixed: true
    - name: fmt
      run: npx prettier --ignore-unknown --write {staged_files}
      stage_fixed: true
