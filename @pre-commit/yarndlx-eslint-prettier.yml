pre-commit:
  parallel: false
  jobs:
    - name: lint
      run: yarn dlx eslint --no-warn-ignored --fix {staged_files}
      stage_fixed: true
    - name: fmt
      run: yarn dlx prettier --ignore-unknown --write {staged_files}
      stage_fixed: true
